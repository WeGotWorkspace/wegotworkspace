{
  "name": "@wgw/workspace",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "sh -c 'if [ -f .env ]; then set -a; . ./.env; set +a; fi; turbo run typegen:watch dev watch --concurrency=20'",
    "dev:php": "sh -c 'if [ -f .env ]; then set -a; . ./.env; set +a; fi; turbo run dev:php --filter=@wgw/dev-server'",
    "dev:ui:watch": "sh -c 'if [ -f .env ]; then set -a; . ./.env; set +a; fi; turbo run typegen:watch watch --filter=@wgw/api --filter=@wgw/admin --filter=@wgw/drive --filter=@wgw/voice --filter=@wgw/mail --filter=@wgw/user-settings --filter=@wgw/install --filter=@wgw/notes --filter=@wgw/home'",
    "build": "sh -c 'if [ -f .env ]; then set -a; . ./.env; set +a; fi; turbo run typegen build'",
    "check:api-types": "sh -c 'if [ -f .env ]; then set -a; . ./.env; set +a; fi; turbo run typegen:check --filter=@wgw/api'",
    "test": "sh -c 'if [ -f .env ]; then set -a; . ./.env; set +a; fi; turbo run test --filter=@wgw/api'",
    "setup:macos": "sh -c 'if [ -f .env ]; then set -a; . ./.env; set +a; fi; pnpm --filter @wgw/api setup:macos'",
    "preview:macos": "sh -c 'if [ -f .env ]; then set -a; . ./.env; set +a; fi; pnpm --filter @wgw/api preview:macos'",
    "update:onlyoffice-web": "bash tools/update-onlyoffice-web-subtree.sh",
    "release": "node tools/build-wegotworkspace-release.mjs",
    "release:wegotworkspace": "pnpm release"
  },
  "devDependencies": {
    "turbo": "^2.4.4"
  }
}
