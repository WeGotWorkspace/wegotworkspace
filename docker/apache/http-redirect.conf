# HTTP → HTTPS when mkcert certs are mounted (enabled in entrypoint).
<VirtualHost *:80>
    ServerName wegotworkspace.local
    DocumentRoot /var/www/install

    RewriteEngine On
    RewriteRule ^ https://wegotworkspace.local%{REQUEST_URI} [R=301,L,NE]
</VirtualHost>
