# Plain HTTP when TLS certs are not mounted (e.g. health on :9080).
<VirtualHost *:80>
    ServerName wegotworkspace.local
    DocumentRoot /var/www/install

    <IfModule mod_dir.c>
        DirectoryIndex index.php
    </IfModule>
</VirtualHost>
