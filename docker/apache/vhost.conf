# Install docroot: front controller + .htaccess (mod_rewrite) like shared hosting.
<Directory /var/www/install>
    Options -Indexes +FollowSymLinks
    AllowOverride All
    Require all granted
</Directory>

# Large uploads (drive / office); tune per host in production.
LimitRequestBody 536870912
